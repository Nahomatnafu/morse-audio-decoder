# Initialize git repo
git init
git add .
git commit -m "Initial commit: ultrasonic Morse code decoder"

# Add remote (replace YOUR_USERNAME and REPO_NAME)
git remote add origin https://github.com/Nahomatnafu/morse-audio-decoder.git
git branch -M main
git push -u origin main